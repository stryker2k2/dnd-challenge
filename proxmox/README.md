# Proxmox Automation

We'll be using Proxmox's Python API

### Setup
```
apt install python3-pip

pip install proxmoxer

# For HTTPS backend (standard)
pip install requests 

# For SSH backend (optional)
pip install paramiko
```